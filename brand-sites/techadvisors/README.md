# BTP TechAdvisors

Deployable PHP 8.3 website for techadvisors.cloudexcelon.com.

Upload the contents of `public_html` to the domain document root or deploy from the matching Git branch `deploy-techadvisors`.

For Plesk, set the deployment path to the website document root so `index.php` lands directly in the served folder.
